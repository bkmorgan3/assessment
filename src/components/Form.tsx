import { CheckboxSection } from "./CheckboxSection";

export default function Form() {
    return <div className="h-max max-w-[880px] min-w-[560px] m-auto">
        <CheckboxSection />
        </div>
}